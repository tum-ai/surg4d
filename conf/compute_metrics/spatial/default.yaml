gt_filename: ${preprocess.spatial_labels_output_filename}
pred_root: ${eval.output_dir}/spatial
output_dir: ${compute_metrics.output_dir}/spatial
aggregated_output_filename: ${compute_metrics.spatial.output_dir}/aggregated.json
output_filename: ${compute_metrics.output_dir}/spatial_metrics.json
l2_error_no_prediction: 500

# which methods to compute metrics for (subset of: frame_attn, frame_attn_refine, frame_direct, graph_agent)
methods:
  - frame_attn
  - frame_attn_refine
  - frame_direct
  - graph_agent